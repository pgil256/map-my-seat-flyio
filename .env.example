# Database Configuration (PostgreSQL)
# For local development
DATABASE_URL=postgresql://username:password@localhost:5432/map_my_seat

# For production (e.g., Neon, Supabase, or other cloud providers)
# DATABASE_URL=postgresql://username:password@host.region.provider.com:5432/database_name?sslmode=require

# Alternative database config (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=map_my_seat

# JWT Secret for authentication
JWT_SECRET=your-secret-key-here
SECRET_KEY=your-secret-key-here

# Node Environment
NODE_ENV=development

# Frontend API URL (for local development)
VITE_API_BASE_URL=http://localhost:3001

# Backend Port
PORT=3001

# CORS Origins (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000